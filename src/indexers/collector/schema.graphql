type Token {
  id: ID!
  address: String!
  symbol: String!
  decimals: Int!
  chainId: Int!
}

type Transfer {
  id: ID!
  from: String!
  to: String!
  value: BigInt!
  token: Token!
  timestamp: Int!
  txHash: String!
}
